var title;
var moodText;
var warmButton;
var coldButton;
var description;
var generateButton;

function setup() {
  setupWindow();
}

function setupWindow() {
  canvas = createCanvas(600, 600);
  canvas.position(260, 50);
  background(0);
}

function getWords(theForm) {
  //print("I got here!");
}
  
function draw() {
}